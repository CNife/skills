---
name: local-github
description: 查 GitHub 仓库的代码、文件或目录结构时，先克隆到 ~/github 参考库再读本地副本，不要用 curl 抓远端文件；~/github 参考库的克隆、同步与路径查询也走这里。
---

# Local GitHub

`~/github` 是**参考库**：第三方仓库各一份长期本地副本，目录名是**规范名** `属主@仓库名`，大小写与 GitHub 一致。

读 GitHub 上的代码默认读副本——能 grep、能整目录读、离线可用；curl 抓远端文件只看得到零散几页，不是起点。

## 拿路径

```bash
~/.agents/skills/local-github/scripts/manage-local-github.py ensure 属主/仓库名
```

stdout 只有本地绝对路径，直接拿去读；副本缺失时它自己克隆。

**完成标准**：拿到一个存在的目录，之后的读取都在这个目录里做。

## 命令

| 命令 | 作用 |
| --- | --- |
| `ensure <仓库>` | 给出本地路径：缺则克隆，有则直接给，不联网 |
| `ensure --fresh <仓库>` | 先抓取并快进当前分支，再给路径 |
| `path <仓库>` | 只查本地，没有就非零退出 |
| `clone <仓库>` | 显式克隆；接受 `属主/仓库名`、`属主@仓库名` 或 GitHub 地址 |
| `sync` | 全库抓取，干净且有上游的做快进（默认子命令，topgrade 每天跑） |
| `list` | 按上次更新时间列出各仓库，标注命名与 origin 问题 |
| `rename` | 把裸名目录规范成 `属主@仓库名` |

`<仓库>` 一律是 `属主/仓库名`，大小写随意。

## 时机

- **新鲜度**：`sync` 每天刷新全库，平时副本最多滞后一天，默认直接 `ensure` 就够。
- **要断言上游此刻的样子**（"上游是不是已经修了"）：用 `ensure --fresh`，或 `gh api` 直接看远端。
- **`--fresh` 遇到脏工作区、分离 HEAD、无上游**：只往 stderr 写一行提示，不快进也不动工作区，路径照给。
- **副本只读**：库里放的是参考资料，改动放在库外的工作副本里；库里一旦有未提交改动，`sync` 会跳过它。
- **根目录**：`LOCAL_GITHUB_ROOT` 环境变量，默认 `~/github`。
